type Spot {
  name: String
  description: String
  modality: String
  isPaid: Boolean
  alwaysOpen: Boolean
  hasCoverage: Boolean
  images: [Upload!]
  equipmentRequired: Boolean
  entryAmount: String
  coordinates: JSON
}

extend type Query {
  getAllSpots: [Spot]
}
